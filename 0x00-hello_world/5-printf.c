#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{	/* print with printf*/
	printf("with proper grammar, but the outcome is a piece of are,\n");
	return (0);
}
